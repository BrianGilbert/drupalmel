; $Id$

This directory contains Module integration files, and will only be loaded if
the related module exists. All module specific hooks should be contained in
their appropriate files.

Example:
  User module related changes should be in a file called 'user.inc'.
